doctype html
html(lang='zh-Hant')

	head

		include ./common/head.pug

	body

		.container

			.feature
				.logo
					| AWESOME
					br
					| SHARE
				.recent
					.section-title RECENT
					.list
						div A
						div B
						div C

			.creator
				.form
					.section-title SHARE
					.group.url
						label Url
						input.field(type='url' name='meta-url')
					.group.title
						label Title
						input.field(type='input' name='meta-title')
					.group.description
						label Description
						textarea.field(name='meta-description')
					.group.image
						input.image-upload(type='file')
						input(type='hidden' name='meta-image')
					.group
						#upload-html.button GO

				.preview
					.card
						.cover
							img
						.title
						.description
